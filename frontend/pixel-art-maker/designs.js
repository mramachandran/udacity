// Select color input
// Select size input

// When size is submitted by the user, call makeGrid()

function makeGrid() {
  // Your code goes here!
  let grid = document.createElement('grid');
  let grid = document.createElement('grid');
  let tr = grid.appendChild(document.createElement('tr'));
     let cl  = tr.appendChild(document.createElement('td'));
  return grid;
}
